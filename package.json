{
  "name": "serverless-triggers",
  "version": "0.1.3",
  "description": "serverless-triggers CLI",
  "author": {
    "name": "Robson Alves",
    "email": "robsonalves.dev@gmail.com",
    "url": "https://robsonalves.dev"
  },
  "bin": {
    "serverless-triggers": "bin/serverless-triggers",
    "sls-triggers": "bin/serverless-triggers",
    "slst": "bin/serverless-triggers"
  },
  "scripts": {
    "format": "prettier --write **/*.{js,json} && standard --fix",
    "lint": "standard",
    "test": "jest",
    "watch": "jest --watch",
    "snapupdate": "jest --updateSnapshot",
    "coverage": "jest --coverage",
    "changelog": "auto-changelog"
  },
  "files": [
    "src",
    "LICENSE",
    "readme.md",
    "docs",
    "bin"
  ],
  "license": "MIT",
  "dependencies": {
    "gluegun": "latest"
  },
  "devDependencies": {
    "jest": "^24.1.0",
    "prettier": "^1.12.1",
    "standard": "^12.0.1"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
